/* src : tb?v=mBoX_JCKZTE&t=17s */

01. python 
02. pyp install
    py -m pip install --upgrade pip /* its worked */
        or 
    pip3 install -U pip virtualenv

        or upgrade pip 

    python.exe -m pip install --upgrade pip

000 pip cache clear  
        pip cache purge
03. virtual on part-2 folder
        .\venv\Scripts\Activate
03. virtualenv
        py -m venv .venv /*100% work */
        Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned   /*100% work */
04. install pip 
        pip install scrapy

05. check scrapy install or not 
    scrapy

part 03 
===============================

01. scrapy startproject < scrappy project name>

part 4
=========

 01. go to scrappy folder on venev
        scrapy genspider driverBaseScrap driverbase.com/inventory/search/austin-tx/acura

02. install ipython shell 
         pip  install ipython 

03. run srapy shell 
        scrapy shell 

04. install psycopg2                  
        pip install psycopg2-binary    /*postgreSQL*/

                or 
        
        pip install mysql-connector-python   /*mysql*/