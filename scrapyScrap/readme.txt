/* src : tb?v=mBoX_JCKZTE&t=17s */

01. python 
02. pyp install
    py -m pip install --upgrade pip /* its worked */
    or 
    pip3 install -U pip virtualenv


000 pip cache clear  
        pip cache purge
03. virtual on part-2 folder
        .\venv\Scripts\Activate
03. virtualenv
        Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned   /*100%*/
04. pip install scrappy

05. check scrapy install or not 
    scrapy

part 03 
===============================

01. scrapy startproject < scrappy project name>